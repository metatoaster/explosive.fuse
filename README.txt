Introduction
============

A tool to mount zip file(s) to a directory and access individual file
entries as normal files without having to unzip contents to some
temporary location.  Also provides a helper library to view contents
within a zip file.
